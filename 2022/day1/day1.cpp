#include<bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    vector<vector<int>> elves;
    string x;
    while (cin >> x) {
        if(x == ""){
            
        }
    }   
    return 0;
}